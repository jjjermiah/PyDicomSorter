repos:
  - repo: local
    hooks:
      - id: pixi-install
        name: Run pixi install
        entry: bash -c 'pixi install && git add pixi.lock'
        language: system
        files: ^pixi\.lock$
